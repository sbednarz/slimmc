# Copo phase A black-box validation

C01-C06 validate initiation, terminal propagation, combination, disproportionation,
transfer to monomer, and A/B/DP/population invariants through the full
`.model -> engine -> slimmc-storage -> pyslimmc` path.
