# Post-examples regression tests

Minimal regressions extracted from issues found while building the examples suite. They are tests, not educational examples.

```bash
python run_regressions.py --slimmc /path/to/slimmc
```
